/**
 * Neighbors and Connected Devices Module for Tawaswl MikroTik Controller
 * Provides functions for:
 * - Fetching MNDP neighbors and IP Hotspot hosts
 * - Filtering and rendering tables
 * - Opening devices directly in new tab (HTTP/HTTPS/custom port)
 * - Copying IP addresses to clipboard
 * - Diagnostics ping and quick preset connection
 */

export function initNeighborsHandlers() {
  // Can attach any event listeners if needed
}
